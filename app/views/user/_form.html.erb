%= form_with(model: user )do |form| %>
    <% if plano.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(user.errors.count, "error") %> prohibited this usuário from being saved:</h2>
  
        <ul>
          <% errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div>
      <%= form.label :description, style: "display: block" %>
      <%= form.text_area :description %>
    </div>
  
    <div>
      <%= form.submit %>
    </div>
  <% end %>