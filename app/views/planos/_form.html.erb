<%= form_with(model: plano) do |form| %>
  <% if plano.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(plano.errors.count, "error") %> prohibited this plano from being saved:</h2>

      <ul>
        <% plano.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :unidade, style: "display: block" %>
    Selecionar uma unidade: 
    <% unidades_array = Unidade.all.map { |unidade| [unidade.nome, unidade.id] } %>
    <%= form.select(:unidades, options_for_select(unidades_array)) %>

  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
